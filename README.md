# Activité Pratique N°1 : Injection des dépendances

## Réalisé par : Abdelhdai Elomari

---

## 🎯 Objectif
Mettre en œuvre le principe du couplage faible à travers la création d’interfaces et leurs implémentations en Java.

---

## 🧩 Partie 1 : Implémentation (points 1 à 4)

Dans cette partie, les étapes suivantes ont été réalisées :

✔ 1. Création de l’interface IDao avec la méthode `getData()`  
✔ 2. Création d’une implémentation de cette interface : `DaoImpl`  
✔ 3. Création de l’interface IMetier avec la méthode `calcul()`  
✔ 4. Création d’une implémentation de cette interface (`MetierImpl`) en utilisant le couplage faible  


## 📂 Structure du projet

src/
└── ma/
└── ensa/
└── di/
├── dao
└── metier


---

## 🔄 État d’avancement

À ce stade, seule la première partie (points 1 à 4) a été réalisée.  
L’injection des dépendances (point 5) sera implémentée dans la suite de l’activité pratique.
